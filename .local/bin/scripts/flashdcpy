#!/bin/sh

loc="$( lsblk | awk '{ print $7 }' | sed "/^$/ d" | fzf )"
[ -z "$loc" ] && exit 1
cploc="$loc/copied"
[ -d "$loc" ] || mkdir -p "$loc"



( notify-send --urgency=normal "Copying" "Copying $1 🤭 " && cp -Ru "$1" "$cploc" && notify-send --urgency=low "Successful" "$1 copied successfully 😁" && exit 0 ) || (notify-send "Failed to copy $1 😥" && exit 1 )
