#!/bin/sh

mbsync -a

unread="$(find "${XDG_DATA_HOME:-$HOME/.local/share}"/mail/*/[Ii][Nn][Bb][Oo][Xx]/new/* -type f | wc -l 2>/dev/null)"

[ ! "$unread" = "0" ] && notify-send notify-send "New Mail" "You got $unread mail/s."
