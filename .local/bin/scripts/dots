#!/bin/sh


# Configurations
rsync -av --delete ~/.config ~/git/repos/dotfiles/

# Binaries
rsync -av --delete ~/.local/bin ~/git/repos/dotfiles/.local/

# Xprofile
cp -uv ~/.zshenv ~/git/repos/dotfiles/
cp -uv ~/.xprofile ~/git/repos/dotfiles/
