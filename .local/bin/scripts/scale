#!/bin/sh

screen=$( xrandr | grep "\sconnected\s" | awk '{ print $1 }' |  dmenu -c -l 10 )
num=$( dmenu -p "Scale to : " -l 10 -c <  "$HOME"/.config/scale )
xrandr --output "$screen" --scale "$num"x"$num"
